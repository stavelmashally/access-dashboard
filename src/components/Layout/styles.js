import { makeStyles } from '@material-ui/core';

export default makeStyles(theme => ({
  root: {
    display: 'flex',
  },
  content: {
    background: '#f1f1f1',
    width: '100%',
    padding: theme.spacing(3),
  },
}));
